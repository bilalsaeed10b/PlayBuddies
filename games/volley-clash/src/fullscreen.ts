// The one copy of this lives in games/_shared/controls/fullscreen.ts — every
// game re-exports it rather than keeping its own, which is what let three of
// the four drift out of sync with each other before this existed.
export * from '@shared/controls/fullscreen';
